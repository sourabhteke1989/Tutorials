# JavaTutorials

## Generating Test Result HTML Report

To generate the test result HTML report, run the following Maven command:

```sh
mvn surefire-report:report
```

This will generate the test report in the `target/surefire-reports` directory.
